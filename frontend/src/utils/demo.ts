/** The seeded demo login. Shared so the landing page's "Simulate the scam"
 *  button and the sign-in screen's shortcut use the same credentials. */
export const DEMO_CREDENTIALS = { email: 'demo@retrace.com', password: 'Demo@123' };
